name: Bash Demo
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Change directory
      working-directory: ./src
      run: | 
        cd css  
  
