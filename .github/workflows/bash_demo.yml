name: Bash Demo
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Create integration folder
      run: | 
        mkdir -p ./integration
        cp src/css/ ./integration
